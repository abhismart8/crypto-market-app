extends layout

block content
  main#main
    h1.ui.center.aligned.header= title
    .ui.container
      table.ui.celled.table.center.aligned
        thead
          tr
            th Rank
            th Logo
            th Name
            th Symbol
            th Price
            th Market Cap
            th All Time High
        tbody
          each crypto in cryptocurrencies
            tr.crypto_element(data-id=crypto.id)(data-rank=crypto.rank)
              td #{crypto.rank}
              td
                img.ui.mini.rounded.image(src=crypto.logo_url)
              td #{crypto.name}
              td #{crypto.symbol}
              td #{parseFloat(crypto.price)*parseFloat(currency)} INR
              td #{crypto.market_cap} INR
              td #{parseFloat(crypto.high)*parseFloat(currency)} INR
        tfoot
          tr
            th(colspan='6')
    footer
      p © #{new Date().getFullYear()} Crypto Market App || Created by 
        a( href="https://github.com/abhismart8") Abhishek Shukla
